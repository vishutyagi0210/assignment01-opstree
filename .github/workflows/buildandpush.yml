name: Build And Push (DevSecOps)

on:
  push:
    branches: ["main"]

jobs:

  security-scans:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 
          
      - name: TruffleHog Secret Scan
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: ${{ github.sha }}
          extra_args: --only-verified

      # 2. Source Code SCA: Scan pom.xml for vulnerable dependencies
      - name: Trivy Filesystem Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH'

  build-and-push:
    needs: security-scans # Won't run if security scans fail
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      GIT_SHA: ${{ github.sha }}
      USERNAME: ${{ vars.DOCKER_USERNAME }}
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      # This saves ~2 minutes per build by caching ~/.m2/repository across runs
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven' 
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with: 
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
     
      - name: Build and Push Service
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          target: production
          tags: |
            ${{ vars.DOCKER_USERNAME }}/java:${{ github.sha }}
            ${{ vars.DOCKER_USERNAME }}/java:latest
          build-args: |
            GIT_SHA=${{ github.sha }}
            APP_VERSION=${{ vars.APP_VERSION }}
          sbom: true
          provenance: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      # 5. Final Image Scanning
      - name: Docker Scout Scan
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ vars.DOCKER_USERNAME }}/java:${{ github.sha }}
          only-severities: critical,high
          exit-code: true # Fail the build if the final image is vulnerable
